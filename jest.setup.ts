// Adds the `@testing-library/jest-dom` custom matchers (toBeInTheDocument,
// toHaveTextContent, etc.) to every test. The import is inert in the node
// environment, so node-only suites are unaffected.
import '@testing-library/jest-dom';
